"use client"; import { useEffect, useState } from "react"; import PageHeader from "@/components/PageHeader"; import AuthGuard from "@/components/AuthGuard"; import DashboardShell from "@/components/DashboardShell"; import { adminApi, User } from "@/lib/api"; export default function AdminUsersPage() { const [users, setUsers] = useState<User[]>([]); const load = () => adminApi.users().then((r) => setUsers(r.users || [])); useEffect(() => { load(); }, []); const deactivate = async (id:string) => { await adminApi.deactivateUser(id); load(); }; return <AuthGuard roles={["admin"]}><PageHeader title="Admin user management" text="View, monitor, and deactivate user accounts." /><DashboardShell><div className="card card-pad"><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>City</th><th></th></tr></thead><tbody>{users.map((u)=> <tr key={u._id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.city}</td><td><button className="btn btn-danger" onClick={()=>deactivate(u._id)}>Deactivate</button></td></tr>)}</tbody></table></div></div></DashboardShell></AuthGuard>; }
