Farmlink plus frontend

This Next.js frontend is wired to the Express + MySQL backend in the backend/ folder.

Setup:
1. copy .env.local.example to .env.local
2. set NEXT_PUBLIC_API_URL=http://localhost:5000/api
3. npm install
4. npm run dev
